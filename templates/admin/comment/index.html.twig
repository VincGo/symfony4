{% extends 'admin/layout.html.twig' %}

{% block body %}
    <table class="table table-striped table-middle-aligned">
        <thead>
        <tr>
            <th scope="col">Auteur</th>
            <th scope="col">Contenu</th>
            <th scope="col">Publié le</th>
            <th scope="col">Action</th>
        </tr>
        </thead>
        <tbody>

        {% for comment in comments %}
            <tr>
                <td>
                    {{ comment.author.fullName }}
                </td>
                <td>
                    {{ comment.content }}
                </td>
                <td>{{ comment.publishedAt|date("d/m/y à h:i:s") }}</td>
                <td>
                    <form action="{{ path('admin_show_comment', {id: comment.id}) }}">
                        <button>Voir</button>
                    </form>
                    <form action="{{ path('admin_delete_comment', {id: comment.id}) }}" method="post">
                        <button type="submit">Supprimer</button>
                    </form>
                </td>
            </tr>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}