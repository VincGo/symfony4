{% if not post.tags.empty %}
    <p class="post-tags">
        {% for tag in post.tags %}
            <span class="label label-default">
                <a class="fa fa-tag" href="{{ path('article_post', {tag: tag.name}) }}">{{ tag.name }}</a>
            </span>
        {% endfor %}
    </p>
{% endif %}