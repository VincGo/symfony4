{% extends 'base.html.twig' %}

{% block body %}
<div class="col-md-5 col-lg-5">
    <!-- artigo em destaque -->
    <div class="featured-article">
        <a href="#">
            <img src="{{ slide1.image.url }}" alt="{{ slide1.image.alt }}" class="thumb">
        </a>
        <div class="block-title">
            <h2>{{ slide1.title }}</h2>
            <p class="by-author"><small>By Jhon Doe</small></p>
        </div>
    </div>
    <!-- /.featured-article -->
</div>
{% endblock %}